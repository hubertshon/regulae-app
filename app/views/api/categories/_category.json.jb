{
  name: category.name,
  id: category.id,
  statement: category.statement,
  image_url: category.image_url,
  color: category.color,
  category_progress: category.category_progress,
  user_id: category.user_id
  
  
  # {
  #   habit_names: category.habit_names,
  #   habit_ids: category.habit_ids
  # } 

}